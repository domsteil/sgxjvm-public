rootProject.name = "oblivium-public"

include ':samples:rng:rng-enclave'
include ':samples:rng:rng-client'

// Optional compositing of oblivium
if (file("$rootDir/oblivium").isDirectory()) {
    includeBuild("$rootDir/oblivium/sgx-jvm-plugin")
    includeBuild("$rootDir/oblivium")
}
